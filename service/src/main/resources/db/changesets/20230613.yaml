# Switch job_id column in jobs table to type UUID
databaseChangeLog:
  - changeSet:
      id: switch job_id column in jobs table to type UUID
      author: js
      changes:
        # jobs table
        - modifyDataType:
            tableName: jobs
            columnName: job_id
            newDataType: UUID
