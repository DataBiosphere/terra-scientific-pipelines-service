# Add a workspace_storage_container_url column to the pipeline_runs table
databaseChangeLog:
  - changeSet:
      id: Add a workspace_storage_container_url column to the pipeline_runs table
      author: mma
      changes:
        - addColumn:
            tableName: pipeline_runs
            columns:
              - column:
                  name: workspace_storage_container_url
                  type: text
